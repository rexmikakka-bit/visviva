# Axis 1.25.5

Changes since Android 1.25.3:

- The price limit in Abyssal Sources now also filters standard modules in Variations. The fitted module remains visible for comparison, and modules with unknown prices are retained.
- Refined the launcher icon with a smaller abyssal triangle placed farther inside the rounded corner.

Download `app-debug.apk` below to install or update the Android sideload build.
